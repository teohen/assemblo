{
  "name": "assemblo",
  "version": "1.0.0",
  "main": "index.ts",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "types": "tsc --noEmit",
    "build": "bun build ./src/**/*.html --outdir ./dist",
    "prod": "bun run lint && bun run types && bun run build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "bun test --inspect"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/chance": "^1.1.7",
    "chance": "^1.1.13",
    "eslint": "^9.39.2",
    "typescript-eslint": "^8.50.1"
  },
  "dependencies": {
    "@types/bun": "^1.3.4"
  }
}